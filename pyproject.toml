[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "landslide-early-warning"
version = "0.1.0"
description = "AI-Based Early Warning & Risk Monitoring for Landslide-Prone Areas"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0"
]
